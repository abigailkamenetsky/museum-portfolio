node_modules/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.log
.vite/
.claude/settings.local.json

# local MCP server config (Blender bridge) - do not commit
.mcp.json

# Blender working files (large, local only)
blender/
!.env.example

# Marble exports (large binaries; regenerate via the API or re-download)
public/museum/*.spz
public/museum/abby-museum-fallback.png
public/museum/abby-museum-mesh.glb

# Raw Marble exports: source artifacts for the web mesh, too large for GitHub.
# The optimized files that actually ship are abby-museum-mesh-web.glb and
# abby-museum-collider.glb.
public/museum/v4-mesh.glb
public/museum/v4-pano.png
